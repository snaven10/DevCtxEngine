# Rust
/target

# Cargo.lock IS committed: this workspace builds a binary, not a library, and
# the release workflow builds it with --locked so what ships is what was tested.

# DevCtxEngine local state / caches
.devctx/
.devai/
.fastembed_cache/
*.db
*.duckdb
*.db-journal
*.db-wal
*.db-shm

# IDE / OS
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store

# Environment
.env
.env.local
