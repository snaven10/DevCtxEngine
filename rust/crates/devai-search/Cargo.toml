[package]
name = "devai-search"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "Search orchestration for DevAI: vector / keyword / hybrid (RRF) + rerank."

[dependencies]
devai-core.workspace = true
devai-store = { path = "../devai-store" }
thiserror.workspace = true
# Only the EmbeddingProvider / Reranker traits are needed — no fastembed/ort.
devai-embed = { path = "../devai-embed", default-features = false }
devai-rerank = { path = "../devai-rerank", default-features = false }
